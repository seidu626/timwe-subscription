{
  "classification_hints": {
    "domains": [
      "security"
    ],
    "likely_hvc_class": "vertical_slice",
    "requires_human_review": true,
    "requires_parallelism": false
  },
  "constraints": [
    "Close the subscription-partner tenantless bypass: add tenant resolution (tenant_key+channel_key via gateway headers, same tenantctx pattern as subscription-external) to subscription-partner routes /api/v1/subscription/{optin,confirm,optout,status}/{partnerRoleId} and GET /api/v1/subscription/list, and to the legacy webhook POST /api/v1/webhooks/timwe/notification; stamp tenant_id/channel_id on all writes; reject tenantless requests. Acceptance: no subscription-partner code path writes rows without tenant_id; go build and tests pass.",
    "Preserve existing source artifacts.",
    "Use capsule-first context for dispatched agents."
  ],
  "context_policy": {
    "hydrate_on_demand": true,
    "max_initial_tokens": 1800,
    "mode": "capsule_first"
  },
  "created_at": "2026-06-10T03:02:22Z",
  "goal_id": "goal-20260610T030222Z-close-the-subscription-p",
  "goals": [
    {
      "id": "G1",
      "priority": "must",
      "statement": "Close the subscription-partner tenantless bypass: add tenant resolution (tenant_key+channel_key via gateway headers, same tenantctx pattern as subscription-external) to subscription-partner routes /api/v1/subscription/{optin,confirm,optout,status}/{partnerRoleId} and GET /api/v1/subscription/list, and to the legacy webhook POST /api/v1/webhooks/timwe/notification; stamp tenant_id/channel_id on all writes; reject tenantless requests. Acceptance: no subscription-partner code path writes rows without tenant_id; go build and tests pass.",
      "success_criteria": [
        "A valid GoalSpec exists before HVC classification.",
        "The requested outcome is implemented with evidence and tests."
      ]
    }
  ],
  "non_goals": [
    "Do not perform unrelated repository-wide rewrites."
  ],
  "risk_level": "high",
  "schema_version": "1.0",
  "source": {
    "conversation_ref": null,
    "prompt_excerpt": "Close the subscription-partner tenantless bypass: add tenant resolution (tenant_key+channel_key via gateway headers, same tenantctx pattern as subscription-external) to subscription-partner routes /api/v1/subscription/{optin,confirm,optout,status}/{partnerRoleId} and GET /api/v1/subscription/list, and to the legacy webhook POST /api/v1/webhooks/timwe/notification; stamp tenant_id/channel_id on all writes; reject tenantless requests. Acceptance: no subscription-partner code path writes rows witho",
    "prompt_hash": "sha256:bcc050466e5ea42d64ba8b5af3c2239911e5b011d56d114368837b17c12321c7",
    "repo_ref": "agent-hub"
  },
  "success_criteria": [
    "A valid GoalSpec exists before HVC classification.",
    "The requested outcome is implemented with evidence and tests."
  ],
  "title": "Close the subscription-partner tenantless bypass: add tenant resolution (tenant_key+channe"
}
